apiVersion: v1
kind: Service
metadata:
  name: titan-service
spec:
  type: NodePort
  selector:
    app: titan
  ports:
    - port: 80            # internal cluster port
      targetPort: 80      # container port
      nodePort: 30081     # exposed node port
